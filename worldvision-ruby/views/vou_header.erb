<div class="navbar navbar-inverse">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a href="/volunteer" class="brand">NEX-T 台灣世界展望會志工翻譯網站</a>
      <div class="nav-collapse collapse">
        <p class="navbar-text pull-right">
        歡迎, <a class="navbar-link" href="#"><%= current_user.account %></a><a href="/logout" class="btn btn-info">登出</a>
        </p>
        <form action="/volunteer" method="GET">
          <ul class="nav">
          <% if @account_trans_type && @account_trans_type == 'both' || @account_trans_type =='chi' %>
            <li class="<%= @trans_type == 'eng' ? '':'active' %>">
              <a href="/volunteer?type=chi" class="letter_type_button" name="chi">中翻英</a>
            </li>
          <% end %>
          <% if @account_trans_type && @account_trans_type == 'both' || @account_trans_type =='eng' %>
            <li class="<%= @trans_type == 'chi' ? '': 'active' %>">
              <a href="/volunteer?type=eng" class="letter_type_button" name="eng">英翻中</a>
            </li>
          <% end %>
          </ul>
        </form>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>
